## A React Project that uses localStorage()

## **Usage**

    npm install && npm start

After installation and server up and running, open a browser and connect to [localhost:3000](https://localhost:3000).
